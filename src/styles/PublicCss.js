import {
    StyleSheet,
} from 'react-native';
import {Utils} from "rn-curiosity";

export const publicCss = StyleSheet.create({
    tabBarText: {fontSize: TextSize.textSize_10},
    tabBarImage: {width: Utils.getWidth(30), height: Utils.getWidth(30)},

})


